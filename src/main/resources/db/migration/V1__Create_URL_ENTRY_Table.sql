--
-- This migration scripts aims to create the URL_ENTRY table and define its constraints.
--
CREATE TABLE URL_ENTRY (
    ID UUID NOT NULL,
    URL_ADDRESS VARCHAR(2000) NOT NULL,
    URL_ALIAS VARCHAR(10) NOT NULL,
    CREATED_AT DATETIME NOT NULL
);

ALTER TABLE URL_ENTRY
ADD CONSTRAINT PKY_URL_ENTRY
PRIMARY KEY(ID);

ALTER TABLE URL_ENTRY
ADD CONSTRAINT UNQ_URL_ENTRY_URL_ADDRESS
UNIQUE(URL_ADDRESS);

ALTER TABLE URL_ENTRY
ADD CONSTRAINT UNQ_URL_ENTRY_URL_ALIAS
UNIQUE(URL_ALIAS);